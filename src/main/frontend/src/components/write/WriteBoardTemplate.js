import React from "react";

const WriteBoardTemplate = ({children}) => {
    return (
        <>
            <div>
                {children}
            </div>
        </>
    );
};

export default WriteBoardTemplate;