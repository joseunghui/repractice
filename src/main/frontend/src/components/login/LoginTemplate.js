import React from "react";

const LoginTemplate = ({children}) => {
    return (
        <>
            <div>
                {children}
            </div>
        </>
    );
};

export default LoginTemplate;