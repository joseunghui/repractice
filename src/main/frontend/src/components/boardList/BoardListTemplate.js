import React from "react";

const BoardListTemplate = ({children}) => {
    return (
        <>
            <div>
                {children}
            </div>
        </>
    );
};

export default BoardListTemplate;